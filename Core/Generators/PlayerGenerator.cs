using UnityEngine;
using UnityEditor;

public class PlayerGenerator
{
    public Party GenerateInitialPlayer(FightConfig config, string name, string icon)
    {
        Party player = new Party();
       
        return player;
    }
}